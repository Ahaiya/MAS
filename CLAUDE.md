# CLAUDE.md

## 项目概述

本项目是一个基于量规（Rubric）的多智能体文本自动评价系统（MAS）。
系统读取结构化评分标准和非结构化文本，通过多智能体协作输出可解释的分数与反馈。

**当前状态：内环（单篇评价流水线）基础骨架已完成，现进入双环迭代阶段。**

- **内环**：完善现有单篇评价流水线，提升稳定性与可审计性。
- **外环**：新建策略优化 Agent，基于批量结果做误差分析、策略提案与版本迭代。
- **联动**：外环读取内环产物，通过更新 bundle / configs 影响内环下一轮运行，两者保持明确边界。

所有讨论与实现均在**真实 LLM 接入**的视角下进行。

---

## 优先阅读文档

| 优先级 | 文件 | 用途 |
|--------|------|------|
| 最高 | `docs/Zen.md` | 项目宪法，架构决策的最终依据 |
| 高 | `docs/V1/MAS_V1_DESIGN.md` | V1 系统设计全景：内环流程、双环架构、外环 I/O、动作空间、推进路线图（持续更新） |
| 参考 | `docs/architecture.md` | Agent 角色、状态机、数据流 |
| 参考 | `docs/research.md` | 数据契约、配置 schema、评测框架 |
| 参考 | `docs/plan.md` | 各阶段历史实现记录 |
| 配置参考 | `docs/Rubric_Guidelines.md` | 量规维度与分档定义 |
| 配置参考 | `docs/Adjudication_Rules.md` | 双评审、裁决与 composite 规则 |
| 输出参考 | `docs/Example.md` | 解释输出的表现形态，不是规则来源 |
---

## 当前工作模式：执行模式（双环迭代）

收到任务后的默认行为：
1. **先读源文件**，不依赖记忆推测，再作答或动手。
2. **先理解再修改**，明确问题根因后才动 `src/` 代码。
3. **所有回答默认使用中文**。
4. **变更记录**：对系统做出的调整，在 `docs/V1/pipeline_review.md` 三、调整与迭代记录中追加。

### 内环工作原则

- 保持 contracts / state machine / trace / policy 的稳定性与可审计性。
- 内环的修改目标是：稳定执行、充分留痕、为外环提供可分析的中间态产物。
- 任何内环修改必须保证 mock 回归测试通过，不破坏现有 bundle 回放能力。

### 外环工作原则

- 外环 Agent 只读取内环产物（`artifacts/eval/*/`），不在运行时干预内环。
- 外环输出的是**策略提案**，最终落地形态是新版 `configs/` bundle，而非直接改代码。
- 外环循环：`Collect → Measure → Diagnose → Propose → Evaluate → Promote/Reject`。
- 策略晋级必须经过 holdout 验证，不允许仅凭训练集表现上线。

---

## 不可逾越的边界

### 内环边界
- `configs/` 是配置的唯一来源，`docs/*.md` 不在运行时被解析。
- 代码中不得出现硬编码的维度名、分档范围、裁决阈值或 composite 公式。
- Agent 之间的数据传递必须使用 `src/contracts/` 中定义的类型。
- 修改 `src/` 代码，必须有明确的逻辑结构问题作为前提，不做无目的重构。

### 外环边界
- 外环 Agent **不得**在单篇运行时直接修改评分结果。
- 外环 Agent **不得**绕过 scorer / adjudicator / router 等内环节点。
- 外环 Agent **不得**在运行时动态修改 rubric / policy / aggregation 配置。
- 所有策略调整必须通过新版 bundle 走完整内环验证，不允许热更新生效。
- 没有 holdout 集上的稳定提升，不允许策略晋级。