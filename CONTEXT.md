# 领域术语表

本文件只是词汇表，不放实现细节、不当规格书用。

## 评价对象与量规

量规有**三层**，这三个词必须严格按下表用。历史上代码把名字整体上移了一级（把 `a1`
叫"一级指标"），已全面纠正——教师拿着量规原文说"A1 是二级指标"，系统必须是同一个意思。

| 层级 | 例子 | 标识符 | 说明 |
| --- | --- | --- | --- |
| **一级指标（domain）** | A 问题认知与分析能力 | 无 | 能力域，共 A–F 六个。**不落配置**——没有消费方，且可从 code 首字母推出 |
| **二级指标（dim）** | A1 复杂问题的识别和界定 | `dim_id`（`a1`） | 一份 `<dim_id>_rubric.yaml` 就是一个二级指标；`--dim` 的粒度，也是产物落盘的一层目录 |
| **观测点** | A1-1 关键变量识别的全面性 | `code`（`A1-1`） | 二级指标内部的评分点，是「唯一最终分」的归属单位；量规原文里没有这一层，是任务特化出来的 |

量规原文见 `docs/rubric.md`（A–F 六个 domain、21 个 dim）。

- **权重（weight）** —— 挂在**观测点**上，决定该观测点在其二级指标汇总分里的份量。
  同一份量规内权重和必须为 1.0，加载时即校验——加了观测点忘了调权重，分数会静默漂移。
- **任务（task）** —— 一套评价场景及其量规集（如 `experiment`），由命令行 `--task`
  在调用现场指定。**不写在任何配置文件里**：改一个 tracked 文件来切任务，每次实验都会
  带一个脏 diff，多任务并行还会互相冲突。
- **提交（submission）** —— 一名学生交上来的一批材料，以学号命名，可含多个文件。
  引擎不把学号当作学生实体，跨轮次追踪不在当前范围内。

## 数据与证据

- **解析（parse）** —— 把一次提交的源文件交给外部文档解析服务，得到版面块，再变成
  带编号的单元。**与评价严格分开**：解析花真实时间和真实钱，一次提交只付一次；
  评价则要在同一份材料上反复迭代。所有格式走同一条解析路径，不按后缀分路——
  否则同一份材料改个后缀，编号锚点指的就不是一回事了。
- **版面块（layout）** —— 解析服务切出的块，自带类型、页码、坐标。进入白名单的版面块
  与单元**一一对应**；被剔除的（页眉、页脚、页码、水印、侧边栏等）不产生单元，
  但按类型计数记在案，不静默丢弃。layout 是外部服务的词，单元是本系统的词。
- **解析结果原件（raw）** —— 解析服务返回的完整响应，原样保存。它是「这份材料到底被
  识别成了什么」的唯一权威。单元存哪些字段因此是**可逆的决定**——将来要坐标、层级、
  置信度，从原件现推即可，不必重新解析任何材料。
- **单元（Unit）** —— 最小可引用片段，一个版面块一个单元。编号全局连续、
  跨文件共享编号空间，每个单元记得自己来自提交里的哪个文件、第几页。
- **数据包（DataPackage）** —— 引擎认的最小输入单元，由带全局连续编号的单元组成，
  另附一段**溯源信息**（何时解析、源文件清单、解析开关、各类型被剔除了多少）。
  引擎不关心它从哪来。
- **编号锚点（unit_ids）** —— 证据的唯一表达方式。模型引用证据时返回单元编号，
  而非复述原文；越界编号直接拒绝。与之相对的旧做法是「自由复述 + 模糊匹配回溯定位」，
  在历史产物上有 14.7% 静默定位失败。
  锚点的定义**不能随代码改动漂移**：所以派生出的数据包与原件一同落盘——只留原件、
  每次现推的话，白名单加一项就会让全体编号平移，历史产物里的编号会悄悄指向别的内容。

## 评价链路

- **评委（rater）** —— 一条独立评价链路的执行者。`rater_1` 与 `rater_2` 各自完成
  「选段 → 取证 → 评分」全过程，互不共享证据；独立性落在「看哪里」这个分歧点上。
- **仲裁者（rater_3 / adjudicator）** —— 双链分歧时介入。看双链完整证据、量规与原文，
  但**看不到双方分数**，以防锚定。
- **一致（consensus）/ 经仲裁（adjudicated）** —— 最终分的两种来源，记在
  `feedback.json` 的 `source` 字段，供教师识别哪些分需要重点复核。

## 配置

- **provider** —— 一个**角色**到一个**模型端点**的绑定（模型名 + 接口地址 + 参数）。
  角色名即 `rater_1` / `rater_2` / `rater_3` / `feedback`。
- **凭证（credential）** —— 属于**厂商账号**，不属于评委角色。这是两个不同的概念：
  多个角色可以共用同一个厂商账号。因此密钥的环境变量名按厂商取
  （`DEEPSEEK_API_KEY`），而非按角色取（旧的 `RATER_1_API_KEY` 混淆了两者，
  导致同一个值要在 `.env` 里重复填写，改密钥时容易漏改）。
- **运行期旋钮（runtime）** —— 并发上限、超时、重试。既不是密钥也不是模型选择，
  统一放在 `model_config.yaml` 的 `runtime` 段。

配置的职责边界是硬的：**`.env` 只装凭证值，其余一切都在 yaml**。任何一侧缺失都直接
报错，不互相兜底——静默降级会让「跑成了单评委却以为跑了双评」这类事故从产物上完全
看不出来。

同一条原则也适用于量规：缺锚点、缺档位、权重和不为 1 一律在加载时报错，不回落默认值。
否则模型会在**完全看不到评分标准**的情况下打分，而产物上一点痕迹都没有。

解析层有自己的配置，不挤进引擎的配置文件：解析服务不是评委角色，parse 也不是引擎的
一部分。哪些版面块进入单元则**不是配置**——它决定的是编号身份而非内容质量，调错了
得到的是一份与历史不可比的材料，而且从配置上看不出来。

**`configs/` 只放程序会读的文件。** 给人看的文档去 `docs/`，手写模板一概不留——模板
必然滞后（曾有一份写着 5 级量表、而真实量规是 4 级），而一份正在跑的真量规永远不会
过期，要抄就抄它。配置路径全部由约定固定，没有 bundle、没有引用解析。
