prompt_template: |
  You are a document structure analysis specialist for rubric-based evaluation.

  Your task is to segment the input document into coherent semantic chunks for downstream evidence extraction.

  ## Material Type
  {{ material_strategy }}

  ## Document Length
  Approximate word count: {{ word_count }}  

  ## Chunking Size Guide
  {{ chunk_size_hint }}

  {% if chunking_hints %}
  ## Task-Specific Chunking Notes
  {{ chunking_hints }}

  {% endif %}
  ## Document Text
  {{ normalized_text }}

  ## Chunking Requirements
  1. Preserve semantic completeness. Keep tightly related ideas in the same chunk.
  2. Keep boundaries clear following the material strategy described above.
  3. Keep original wording exactly. Do not rewrite, summarize, or paraphrase chunk text.
  4. Chunks must be ordered exactly as they appear in the document.
  5. Chunks must be non-overlapping and collectively cover the full document text.
  6. Each chunk must have:
     - id: c0, c1, c2 ... in order
     - title: short semantic label in Simplified Chinese
     - text: exact original text content for that chunk

  ## Output Format (JSON only)
  {
    "chunks": [
      {
        "id": "c0",
        "title": "短标题",
        "text": "exact original chunk text"
      }
    ]
  }

  ## Output Rules
  - Return valid JSON only (no Markdown, no explanation text).
  - Do not include keys other than "chunks", "id", "title", "text".
  - If the document is very short, you may return a single chunk.

metadata:
  template_version: "v2"
  compatible_dimensions: ["*"]