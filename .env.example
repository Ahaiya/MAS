#   cp .env.example .env   然后填入真实值。.env 已在 .gitignore 里，切勿提交。
#
# 这里**只放密钥值**。模型、api_base、超时、重试、并发全部在
# configs/model_config.yaml —— 职责不重叠，改模型不用碰 .env。
#
# 变量名按**厂商**取而非按角色：凭证属于厂商账号，多个评委角色共用同一账号时
# 不必把同一个值抄好几遍。要新增厂商，在 model_config.yaml 里给对应 provider
# 写上新的 api_key_env 名字，再到这里加一行即可。

# DeepSeek 账号
DEEPSEEK_API_KEY=

# 阿里云百炼（DashScope）账号
DASHSCOPE_API_KEY=

# 阿里云账号 AK/SK —— 解析层（Document Mind 文档解析）用。
# 注意：这是**两个值**，且与上面的 DASHSCOPE_API_KEY（百炼）**不是同一套凭证**，
# 不要把百炼的 key 填到这里。控制台：访问控制 → 用户 → AccessKey。
ALIBABA_CLOUD_ACCESS_KEY_ID=
ALIBABA_CLOUD_ACCESS_KEY_SECRET=
